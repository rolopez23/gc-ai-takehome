# Backend .env — used by FastAPI / uvicorn
# Copy this file to .env and fill in your values:
#   cp .env.tmpl .env

DATABASE_URL=postgresql+asyncpg://app:app@localhost:5432/app   # Matches docker-compose.yml defaults
ANTHROPIC_API_KEY=sk-ant-...                                    # Required — get one at https://console.anthropic.com/
