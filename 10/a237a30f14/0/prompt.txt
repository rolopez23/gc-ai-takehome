Ok we want to launch /problem-spec right now we just do a naive call with a prompt to handle our agentic.  I want a true agentic workflow.  We will have the following flow.  Contract comes in => Split into clauses => Each Clause Evaluated => Synthesis.  That way we can use tool calls to guarantee the formatting of each sub agent and the final format.  We can also check the fairness.  Similarly a sub-agent to generate a minimal headline.  Also this will allow the orchestration to send back better information.  I am thinking of upgrading our call to use streaming http (not pure SSE) where we hit the endpoint and get back a stream.  Thoughts?

---

1. Parrallel better UX/UI.  Headline is same as summary and call_to_action lets tighten up those and make them agents.  This will also let us tighten the prompts.  I would revive the playbook too and make a get playbook tools that allows agents to intelligently find rules and thoughts based on their clause.

---

you have to pull the playbook out of git history it was in the old but we deprecated you should have made a note of where to find it.

---

I would say lets determine when we split the clauses based on the intial read and the contract.

---

So they cover different material I think.  I do like ours for now.  I would do some mapping Triggered means it is relavnt (that is only if we did) Pass => Fair, Absent => , Partial => non-standard.  Lets track both on the information since the data is cheap, keep relying on our dealbreaker, non-standard, standard tiers.

---

that sounds good, lets also have teh started return empty text and support a token event to update add text.

---

I think that is a good point on headline.  During streaming we will have headline be a status.  We will do a replace event after to change it to the finalized version generated by a tool call.  2] Fall back to general agreement. 3. Have the LLM generate the inputs that the playbook needs (type of contract), what may be relevant.  It should come at the first pass.  For clauses referencing I think the splitter needs to indicate that, so we can use. I think after teh splitter we should persist clauses in the db so anyone can access.  I like making sythensis deterministic.   Yes I think that is a good callout.  The streaming upgrade will require frontend chagnes.  I was trying to minimize because of another agent but lets say minimum possible frontend changes to go from streaming to polling.

---

yes I like that splitter determines agreement type and flags relevant checks.

---

Lets do another loop on the specification in and re-run all steps.

---

Ok interview me 1 question at a time to resolve this ambiguity.  Feel free to add more questions if they come up and favor more questions rather than assumptions.

---

Lets give the splitter a list of names and a 1 sentence when to apply.  It can only apply valid playbook items.

---

I like a clean endpoint, this is such a new flow lets handle it that way, also that means we will not break the frontend until we point it at a new endpoint.  I would call at that we want to keep the old endpoint clean.

---

hmmm lets actually have the splitter check for cycles and check for entire agreement.  If it is missing context that is a high levle failure that should be referenced.

---

Call out on clause if it is part of a cycle so that the reviewer knows.  Also as a guard data only goes to depth 1 on references for now.

---

All.  I would preface the system prompt for each agent to reference the source of the information and highglight especially on evaluators (instructions may not be relevant for this clause, if irrelevant ignore).

---

Oh I like that call_to_action at the end.  It should be really minimal.  Summary can be where the in-process material goes and we replace at the end.

---

Lets build a 2x retry into fails for LLM calls.  I would mark it as failed and bass on the information we have.  Ignore it for the final status (it is like the clause does not exist).  If more that 10% of clauses fail we do cause a failure.

---

Sure if that increases reliablity, feel free to back off it is causing issues

---

syntehic number

---

this is a new api, lets make it use new schemas since there are breaking changes.

---

we have a docker postgres container I believe

---

Lets change the sort to be more semantic, we acutally now show by bucket, secondary is out of order.  I think that spec is wrong we want to show from most to least egregious as buckets, with ordering not as important inside.  I will add frontend sort features.  I would add a 1-10 score like playbooks on each item as this could be useful to sort.

---

Sure! It is low value but lets be consistent.

---

Full for now

---

Lets have it before teh spliiter.  Another contract verification test.

---

it does not need to read the full document deeply.  It is meant to be a lightweight pass on a simple model.  Lets also make agents configurable for different models.

---

Either is fine, use your taste

---

I would not worry about that add disconnection todo.  Basically they would not see the contract until it appers on the home page, parrallel work.

---

I like that that has been an issue.  lets double for now

---

yes status failed we can say evaluators failed.  Don't worry about the users we are updating error codes elsewhere

---

lets have the splitter run those checks good call out.

---

it should

---

lets stress test it one more time we are wigguming for a bit

---

yes

---

Good callout lets skip.

---

Hmm lets double budget for now.  Lets track token errors.

---

I think it is simper to drop, do you agree?

---

yes!

---

that works

---

that can work too,

---

yes

---

the simplest is a status message I think.

---

Yes

---

yes lets max at 7

---

flagged as a finding

---

what do you think?

---

we won't get anything by converting, so I think we have to pass on what we have.

---

Ok what are teh open questions?

---

Lets do 20% for now.  Make it easy to change. [Image #1] does that show my rate limits?

---

Ok lets plan I am leanding towards using lang chain for the agentic orchestaration, but open to other options.

---

Ok this looks good for the first pass, so langeChacin is overkill at this point?

---

Ok are we adding tools to make sure we have deterministic wrappers around the formatting for the contract information?

---

Yes

---

Do we have parrelization sorted out?

---

I mean for the planning how is the parrelization for that?

---

No I mean for teh plan what agents can we run in parrelel, will one claude orchestrate, should I open a second session? Worktrees or everything on one branch with isolated files?

---

That sound good but make sure that all agents follow the tests=>code=>verify=>simplify=>review pipeline updating the plan at every step.  That has failed in the past.

---

just checking I see three agents running but I have not seen the plan update want to confirm we are adhering to updating plan on commits and running the full workflow

---

relaunch

---

VERIFY->SIMPLIFY->REVIEW is also per cycle

---

Also we want Update to happen in teh cycle after each step, that makes it easier to monitor

---

slightly different UPDATE-PLAN SHOULD Happy after 2, after, 5, after, 6 and after 7.  Also commit when you update the plan.

---

That works!

---

Yes lets review with a walkthrough of the three steps combined.

---

snippets

---

we are upading our data model, to accomodate the major shift. status we need to udpate and we can initially set to pending. Serverity requires input so we null it.  Batch alter table I am less familair with

---

Do we need batch_alter_table if we are using postgres back to question 1

---

is our application running on postgres or sql_lite?

---

lets fix this shit

---

show me the description and code

---

The first is the original columns.  The second is everything we added for pipleline concerns.

---

we always want a section number and clause type, as they are generated by splitter to make a clause, the other pieces are from later agents.

---

hmmm should we not also split the backend data, this would break it like create new versions that the streaming endpoint uses and old versions that the polling endpoints use?

---

will it break the frontend if we send extra or does zod just ignore?

---

I think we are ok, but that is a good gotcha we should cover be a comment on the risk in the transitory state.

---

can you show the question again

---

show me the code!

---

So the pydantic will now diverve teh lower parts will not match the old schema (we will have new ones).  We need none on caluses because we enter a clause now before it was evaluated two steps where we had one.

---

I am not sure on the order dive deeper, what is the context manager are we loading the playbook into context for the llm?

---

ok so it makes the playbook a dependency of the server, and creates an in memory cache of this data, cool

---

oh so we are building a cache of teh plyabook. This is because it is often used.  Cool.

---

it would raise a key aeror.  For the feirst part, the checks by name would raise a value error if they did not match

---

you are missing the lines in your readout, but I athink auto lets the model decide on tool calls as opposed to requiring them

---

hmm I think you are being to generous again, low readings are not bad they are a sign of learning to come don't feel bad about giving them out.  Re-evaluate

---

Save in the todo! Thank you for pushing me to learn.  Also add some resources for me to review

---

Defer sign off, I will do that at the end for all steps.

---

yes launch those agents

---

I am losing steam lets go to phase 3

---

wait I see no files for verify / simply / or review are we running these steps? how do I know?

---

these should be skills that create reports. I think you are doing lightweight versions.

---

lets take the current state of the code and run verify simplify and review on it.

---

of course that is the point of verify also are also is the database verified (by going into the docker cotnaienr).

---

Tell me the real verification your an for aeach stepl

---

Ok verification is not tests or scanning, it is running the code and showing it works.  IE looking at a databse.  What steps did you run code? Plumbing cannot be verified until later unless you get creative.  The lru cache could be accessed though I imagine.

---

Also booting the server would tell if the playbook runs.

---

yes!

---

yes feel free to spin up multiple agents if you can we have lots to do!

---

continue!

---

yes!

---

lets do the right way kick off /verify

---

how did you verify the pipeline step? Just want to confirm

---

Ok that makes sense, in good shape to run simplify

---

yes

---

Yes

---

Yes! Feel free to review the plan and see if tehre are any mini-cycles you can break out it is fairly big

---

are you hitting the right endpoint? shouldn't stream be a new endpoint?

---

I think they should both exist but the frontend should call one of the other deliberately.

---

I like the query parameter

---

lets run /simplify and /review if you have not

---

Ok can we break down teh frontend further? We are going late tonight due to 9am deadline tommorrow.

---

Update teh plan also state that after parralel agents merge is the time to do verify simplify review.  Those are skills and cannot just be marked.